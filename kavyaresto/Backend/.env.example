# Example .env file for Backend
# Copy to .env and update values before running

PORT=5000
MONGO_URI=mongodb://localhost:27017/kavyaresto
JWT_SECRET=your_jwt_secret_here

# Optional - Gmail SMTP credentials (if not provided, backend will log OTP to console)
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_app_password_or_smtp_password

# Frontend origin allowed by CORS (match your dev server)
CORS_ORIGIN=http://localhost:5173

